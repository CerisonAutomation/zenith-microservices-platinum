# NOTE: This is a template. In production, use proper secret management (e.g., Sealed Secrets, External Secrets Operator)
# DO NOT commit actual secrets to version control
apiVersion: v1
kind: Secret
metadata:
  name: zenith-secrets
type: Opaque
stringData:
  # Database Credentials
  POSTGRES_USER: "zenith"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  DATABASE_URL: "postgresql://zenith:CHANGE_ME_IN_PRODUCTION@postgres-service:5432/zenith"

  # Redis Password
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # JWT Secrets
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION_USE_STRONG_RANDOM_STRING"

  # API Keys
  STRIPE_SECRET_KEY: "sk_live_CHANGE_ME"
  STRIPE_WEBHOOK_SECRET: "whsec_CHANGE_ME"

  # OAuth Credentials
  GOOGLE_CLIENT_ID: "CHANGE_ME"
  GOOGLE_CLIENT_SECRET: "CHANGE_ME"
  GITHUB_CLIENT_ID: "CHANGE_ME"
  GITHUB_CLIENT_SECRET: "CHANGE_ME"

  # Email Configuration
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  SMTP_USER: "CHANGE_ME"
  SMTP_PASSWORD: "CHANGE_ME"

  # Supabase Configuration
  SUPABASE_URL: "https://your-project.supabase.co"
  SUPABASE_ANON_KEY: "CHANGE_ME"
  SUPABASE_SERVICE_ROLE_KEY: "CHANGE_ME"
