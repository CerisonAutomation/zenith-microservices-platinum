# Zenith Dating App - Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: Never commit .env.local to version control!

# ============================================
# DEMO MODE vs PRODUCTION MODE
# ============================================
# If you don't set SUPABASE credentials below, the app will run in DEMO MODE
# with mock data. This is perfect for testing the UI/UX.
#
# To enable PRODUCTION MODE with real backend integration:
# 1. Set up a Supabase project at https://supabase.com
# 2. Fill in the SUPABASE_URL and SUPABASE_ANON_KEY below
# 3. Run the database migrations from /migrations/
# 4. Optionally configure microservices endpoints

# ============================================
# SUPABASE CONFIGURATION (Required for Production)
# ============================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ============================================
# AI SERVICES (For Virtual Boyfriend Feature)
# ============================================
# Google Gemini API (Primary AI for chat bot)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=

# Alternative AI Providers (Optional)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ============================================
# VIDEO/VOICE CALLING (Required for calls feature)
# ============================================
# Daily.co API for WebRTC video/audio calls
# Get from: https://dashboard.daily.co/developers
# FREE tier: 10,000 minutes/month
DAILY_API_KEY=

# ============================================
# GIF SUPPORT (Required for GIF messages)
# ============================================
# Giphy API for GIF search and integration
# Get from: https://developers.giphy.com/dashboard
# FREE tier: 1,000 requests/day
NEXT_PUBLIC_GIPHY_API_KEY=

# ============================================
# MICROSERVICES ENDPOINTS (Optional)
# ============================================
# If not set, the app will fall back to Supabase direct access
# Default ports are shown - adjust if your services run on different ports

# API Gateway (orchestrates all microservices)
NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:3100

# Individual Services
NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:3001
NEXT_PUBLIC_PAYMENT_SERVICE_URL=http://localhost:3002
NEXT_PUBLIC_DATA_SERVICE_URL=http://localhost:3003
NEXT_PUBLIC_I18N_SERVICE_URL=http://localhost:3004

# ============================================
# STRIPE PAYMENT CONFIGURATION (Optional)
# ============================================
# Required for subscription and payment features
# Get from: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ============================================
# OAUTH PROVIDERS (Optional)
# ============================================
# Configure these in your Supabase project first
# Supabase Dashboard > Authentication > Providers

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Apple OAuth
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# ============================================
# APPLICATION SETTINGS
# ============================================
# Next.js specific
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Booking a Boyfriend"

# Enable development mode (allows access without auth for testing)
NEXT_PUBLIC_DEV_MODE=false

# ============================================
# FEATURE FLAGS (Optional)
# ============================================
NEXT_PUBLIC_ENABLE_VIDEO_CALLS=true
NEXT_PUBLIC_ENABLE_VOICE_MESSAGES=true
NEXT_PUBLIC_ENABLE_AI_BOYFRIEND=true
NEXT_PUBLIC_ENABLE_EMOJI_REACTIONS=true
NEXT_PUBLIC_ENABLE_STORIES=true
NEXT_PUBLIC_ENABLE_GIF_SUPPORT=true
NEXT_PUBLIC_ENABLE_PREMIUM_FEATURES=true
NEXT_PUBLIC_ENABLE_GEOLOCATION=true

# ============================================
# ROLES & PERMISSIONS
# ============================================
# Available roles: user, boyfriend, admin
# Users can book boyfriends, boyfriends can accept bookings, admins manage platform

# ============================================
# ANALYTICS & MONITORING (Optional)
# ============================================
# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Sentry Error Tracking
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
# For sending transactional emails
# Supabase uses SMTP or can integrate with SendGrid, etc.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@bookingaboyfriend.app
SMTP_FROM_NAME="Booking a Boyfriend"

# ============================================
# STORAGE CONFIGURATION (Optional)
# ============================================
# Supabase Storage is used by default for:
# - Profile photos
# - Voice messages
# - Story media
# - Chat attachments

# AWS S3 (Optional alternative to Supabase Storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=

# Cloudinary (Optional for image optimization)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ============================================
# SECURITY & RATE LIMITING
# ============================================
# SECURITY FIX #4: JWT Secret must be generated and set securely
# NEVER use default or example values in production
# Generate a secure random secret using: openssl rand -base64 32
# This is handled by Supabase Auth in production - only needed for custom microservices
JWT_SECRET=

# Rate limiting (requests per minute)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# SECURITY FIX #3: CORS allowed origins (comma-separated)
# IMPORTANT: Restrict to your actual domains in production
# Never use wildcard (*) in production
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://zenith.app,https://www.zenith.app

# ============================================
# DATABASE (Only needed if running services locally)
# ============================================
# Supabase provides PostgreSQL, but if running your own:
DATABASE_URL=postgresql://user:password@localhost:5432/zenith
DIRECT_URL=postgresql://user:password@localhost:5432/zenith

# ============================================
# REDIS (Optional - for caching and sessions)
# ============================================
REDIS_URL=redis://localhost:6379

# Upstash Redis (Vercel-recommended, has free tier)
# Get from: https://console.upstash.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ============================================
# TWILIO (Optional - for SMS verification)
# ============================================
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ============================================
# GEOLOCATION API (Optional)
# ============================================
# For reverse geocoding and location services
GOOGLE_MAPS_API_KEY=
MAPBOX_ACCESS_TOKEN=

# ============================================
# TEXT-TO-SPEECH (Optional - for voice AI boyfriend)
# ============================================
# ElevenLabs for high-quality voice synthesis
ELEVENLABS_API_KEY=

# ============================================
# SPEECH-TO-TEXT (Optional - for voice messages)
# ============================================
# OpenAI Whisper API for transcription
OPENAI_WHISPER_API_KEY=

# ============================================
# DEVELOPMENT & DEBUGGING
# ============================================
# Show detailed error messages
NODE_ENV=development

# Enable Next.js debug mode
DEBUG=false

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# VERCEL-SPECIFIC OPTIMIZATIONS (Free Features)
# ============================================
# These are automatically enabled on Vercel:
# - Edge Functions
# - Image Optimization
# - Analytics
# - Web Vitals
# - Preview Deployments
# - Automatic HTTPS
# - CDN caching

# ============================================
# SUPABASE FREE TIER LIMITS
# ============================================
# Free tier includes:
# - 500 MB database space
# - 1 GB file storage
# - 2 GB bandwidth
# - 50,000 monthly active users
# - Unlimited API requests
# - Real-time subscriptions
# - Row Level Security
# - Auto-generated APIs

# ============================================
# NOTES
# ============================================
# QUICK START:
# 1. Copy this file to .env.local
# 2. For DEMO MODE: Leave Supabase credentials empty
# 3. For PRODUCTION: Fill in at minimum:
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - SUPABASE_SERVICE_ROLE_KEY
#    - GOOGLE_GEMINI_API_KEY (for AI boyfriend)
#    - DAILY_API_KEY (for video calls)
#    - NEXT_PUBLIC_GIPHY_API_KEY (for GIFs)
# 4. Run: pnpm install && pnpm dev
# 5. Open: http://localhost:3000
#
# The app will automatically detect if Supabase is configured and switch
# between demo mode (mock data) and production mode (real backend).

# ============================================
# SECURITY REMINDER
# ============================================
# - NEVER commit .env.local to git
# - NEVER expose SUPABASE_SERVICE_ROLE_KEY publicly
# - NEVER hardcode API keys in code
# - NEVER use default/example values in production
# - Use NEXT_PUBLIC_ prefix only for client-safe variables
# - Rotate keys regularly in production
# - Use different keys for dev/staging/production
# - Generate secure random secrets: openssl rand -base64 32
# - Validate all environment variables on startup
# - Enable audit logging in production
# - Use environment-specific .env files (.env.production, .env.staging)
# - Store secrets in secure vaults (AWS Secrets Manager, Vault, etc.) in production
