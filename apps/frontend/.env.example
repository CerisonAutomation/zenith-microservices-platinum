# Zenith Dating App - Environment Configuration Template
# Copy this file to .env.local and fill in your actual values

# ============================================
# DEMO MODE vs PRODUCTION MODE
# ============================================
# If you don't set SUPABASE credentials below, the app will run in DEMO MODE
# with mock data. This is perfect for testing the UI/UX.
#
# To enable PRODUCTION MODE with real backend integration:
# 1. Set up a Supabase project at https://supabase.com
# 2. Fill in the SUPABASE_URL and SUPABASE_ANON_KEY below
# 3. Run the database migrations from /migrations/
# 4. Optionally configure microservices endpoints

# ============================================
# SUPABASE CONFIGURATION (Required for Production)
# ============================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ============================================
# MICROSERVICES ENDPOINTS (Optional)
# ============================================
# If not set, the app will fall back to Supabase direct access
# Default ports are shown - adjust if your services run on different ports

# API Gateway (orchestrates all microservices)
NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:3100

# Individual Services
NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:3001
NEXT_PUBLIC_PAYMENT_SERVICE_URL=http://localhost:3002
NEXT_PUBLIC_DATA_SERVICE_URL=http://localhost:3003
NEXT_PUBLIC_I18N_SERVICE_URL=http://localhost:3004

# ============================================
# STRIPE PAYMENT CONFIGURATION (Optional)
# ============================================
# Required for subscription and payment features
# Get from: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ============================================
# OAUTH PROVIDERS (Optional)
# ============================================
# Configure these in your Supabase project first
# Supabase Dashboard > Authentication > Providers

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Apple OAuth
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# ============================================
# APPLICATION SETTINGS
# ============================================
# Next.js specific
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Zenith Dating"

# Enable development mode (always allows access without auth)
NEXT_PUBLIC_DEV_MODE=true

# ============================================
# FEATURE FLAGS (Optional)
# ============================================
NEXT_PUBLIC_ENABLE_VIDEO_CALLS=true
NEXT_PUBLIC_ENABLE_AI_MATCHING=true
NEXT_PUBLIC_ENABLE_PREMIUM_FEATURES=true
NEXT_PUBLIC_ENABLE_GEOLOCATION=true

# ============================================
# ANALYTICS & MONITORING (Optional)
# ============================================
# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Sentry Error Tracking
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
# For sending transactional emails
# Supabase uses SMTP or can integrate with SendGrid, etc.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@zenith.app
SMTP_FROM_NAME="Zenith Dating"

# ============================================
# STORAGE CONFIGURATION (Optional)
# ============================================
# Supabase Storage is used by default
# These are only needed if using external storage providers

# AWS S3 (Optional alternative to Supabase Storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=

# Cloudinary (Optional for image optimization)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ============================================
# SECURITY & RATE LIMITING
# ============================================
# JWT Secret (used by auth service)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Rate limiting (requests per minute)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================
# DATABASE (Only needed if running services locally)
# ============================================
# Supabase provides PostgreSQL, but if running your own:
DATABASE_URL=postgresql://user:password@localhost:5432/zenith
DIRECT_URL=postgresql://user:password@localhost:5432/zenith

# ============================================
# REDIS (Optional - for caching and sessions)
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# TWILIO (Optional - for SMS verification)
# ============================================
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ============================================
# GEOLOCATION API (Optional)
# ============================================
# For reverse geocoding and location services
GOOGLE_MAPS_API_KEY=
MAPBOX_ACCESS_TOKEN=

# ============================================
# AI/ML SERVICES (Optional)
# ============================================
# For AI matching and recommendations
OPENAI_API_KEY=

# ============================================
# DEVELOPMENT & DEBUGGING
# ============================================
# Show detailed error messages
NODE_ENV=development

# Enable Next.js debug mode
DEBUG=false

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# NOTES
# ============================================
# QUICK START:
# 1. Copy this file to .env.local
# 2. For DEMO MODE: Leave Supabase credentials empty
# 3. For PRODUCTION: Fill in at least SUPABASE_URL and SUPABASE_ANON_KEY
# 4. Run: npm install && npm run dev
# 5. Open: http://localhost:3000
#
# The app will automatically detect if Supabase is configured and switch
# between demo mode (mock data) and production mode (real backend).
