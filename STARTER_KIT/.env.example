# ZENITH Environment Configuration - Supabase Native Backend

# === Supabase Configuration ===
# Get these from your Supabase project dashboard
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# === Database (Supabase PostgreSQL) ===
# Use Supabase connection string (optional, for direct SQLAlchemy access)
DATABASE_URL=postgresql://postgres:[password]@db.your-project-id.supabase.co:5432/postgres
SQLALCHEMY_DATABASE_URL=postgresql://postgres:[password]@db.your-project-id.supabase.co:5432/postgres

# === JWT/Security (Handled by Supabase Auth) ===
# These are now managed by Supabase - no need to configure manually
# SECRET_KEY=handled-by-supabase
# ALGORITHM=handled-by-supabase
# ACCESS_TOKEN_EXPIRE_MINUTES=handled-by-supabase

# === Storage (Supabase Storage) ===
# File storage is now handled by Supabase Storage
# No need for AWS S3 configuration - all files stored in Supabase buckets
# SUPABASE_STORAGE_BUCKET=your-bucket-name

# === Email Configuration (Supabase Auth) ===
# Email sending is handled by Supabase Auth
# Custom SMTP configuration no longer needed for basic auth emails
# For advanced email features, use Supabase Edge Functions

# === Payment (Stripe remains external) ===
STRIPE_API_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# === Feature Flags ===
DEBUG=False
DEMO_MODE=True
LOG_LEVEL=INFO

# === CORS ===
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173,https://zenith.app

# === Admin (Supabase Auth) ===
# Admin users are now managed through Supabase Auth dashboard
# ADMIN_EMAIL=admin@zenith.app
# ADMIN_PASSWORD=handled-by-supabase
